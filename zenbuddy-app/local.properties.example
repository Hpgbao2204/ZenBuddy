sdk.dir=/path/to/android/sdk

# Optional AI features: food image scan, meal plans, workout plans, health chat.
# Get a key from Google AI Studio, then paste it here without quotes:
GEMINI_API_KEY=your-gemini-api-key

# Optional weather key is not required. Weather uses Open-Meteo by default.
